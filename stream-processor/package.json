{
  "name": "stream-processor",
  "version": "1.0.0",
  "description": "Reads from Redis Streams, writes metrics to InfluxDB and logs to Elasticsearch",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "ioredis": "^5.3.2",
    "@influxdata/influxdb-client": "^1.33.2",
    "@elastic/elasticsearch": "^8.11.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
